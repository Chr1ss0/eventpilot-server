export type CustomErrType = {
  code: number;
};

export type CloudUrlType = {
  secure_url: string;
  public_id: string;
};
export type LocationType = {
  placeName: string;
  state: string;
  coordinates: number[];
};
